'use strict';

var namespace = 'd';

var day = 'day';
var dayComponent = namespace + '-' + day;

var month = 'month';
var monthComponent = namespace + '-' + month;

var calendar = 'calendar';
var calendarComponent = namespace + '-' + calendar;

var rangePicker = 'rangepicker';
var rangePickerComponent = namespace + '-' + rangePicker;

module.exports = {
  namespace: namespace,

  day: day,
  dayComponent: dayComponent,

  month: month,
  monthComponent: monthComponent,

  calendar: calendar,
  calendarComponent: calendarComponent,

  rangePicker: rangePicker,
  rangePickerComponent: rangePickerComponent
};